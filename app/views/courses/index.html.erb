<div class="w-full">
  <% if notice.present? %>
    <p class="inline-block px-3 py-2 mb-5 font-medium text-green-500 rounded-lg bg-green-50" id="notice"><%= notice %></p>
  <% end %>

  <div id="courses" class="flex flex-wrap justify-center min-w-full gap-14">
    <%= render @courses %>
  </div>
</div>
